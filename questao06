#include <stdio.h>

int main() {
    float raio;
    
    printf("Digite o raio: ");
    scanf("%f", &raio);
    
    float area = 3.14 * raio * raio;
    
    printf("A área do círculo é: %.2f\n", area);
    
    return 0;
}

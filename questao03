#include <stdio.h>

int main(void) {

  int base;
  int altura;
  int area;
  
  printf("Medida da base: ");
  scanf("%d", &base);
  printf("Medida da altura: ");
  scanf("%d", &altura);

  area = base * altura;

  printf("A área da sua sala é: %d", area);

}
